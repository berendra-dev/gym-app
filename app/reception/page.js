'use client'

import ReceptionistPage from '../receptionist/page'

export default function ReceptionPage() {
  // Alias route: renders the receptionist dashboard at /reception (canonical path)
  return <ReceptionistPage />
}
